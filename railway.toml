# Railway config (repo root). Dockerfile CMD runs uvicorn directly (no start.sh).
# UI settings override this file when set — see FAIRE-MAINTENANT-RAILWAY.md
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
